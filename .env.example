# Copy this file to `.env.local` and fill in your values.
# Get your connection string from Neon: Project → Connect → Connection string
VITE_DATABASE_URL=postgresql://ohmie_app:YOUR_PASSWORD@ep-steep-sea-b3tod37c-pooler.c-4.ap-southeast-1.aws.neon.tech/ohmie?sslmode=require

# Optional live-AI explainers in the PYQ quiz. Point these at any
# OpenAI-compatible /chat/completions endpoint. Without them, the built-in
# template explainer is used.
# VITE_AI_ENDPOINT=https://api.openai.com/v1/chat/completions
# VITE_AI_API_KEY=sk-...
# VITE_AI_MODEL=gpt-4o-mini